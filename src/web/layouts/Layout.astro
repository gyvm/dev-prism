---
import { sidebarHtml, SIDEBAR_STYLES, type SidebarActive } from "../shell/sidebar.ts";

interface Props {
  title?: string;
  active?: SidebarActive;
}

const { title = "PR Analytics", active = null } = Astro.props;
// BASE_URL mirrors the configured base and is NOT guaranteed to end in "/"
// (e.g. "/pr-weekly-report" with no trailing slash). Normalize so the gallery
// link is the base root and Explore is a clean child path.
const raw = import.meta.env.BASE_URL;
const base = raw.endsWith("/") ? raw : `${raw}/`;
const links = { reportsHref: base, exploreHref: `${base}explore`, active };
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <style is:global set:html={SIDEBAR_STYLES}></style>
  </head>
  <body>
    <Fragment set:html={sidebarHtml(links)} />
    <slot />
    <script>
      import { mountSidebar } from "../shell/sidebar.ts";
      mountSidebar();
    </script>
  </body>
</html>
