node_modules
coverage
dist/reports
dist/index.html
dist/manifest.json
.DS_Store
.env
.env.*
tmp/
*.png
!docs/images/*.png
.playwright-mcp

# Real PR data — never commit. data/demo/ is the only tracked subtree.
data/raw/
data/analysis/
data/history/
data/latest.json

# Per-user config (commit config.toml.example as the template)
config.toml
